<% add_decidim_meta_tags(title: t("contact", scope: "menu")) %>
<div class="small-centered columns">
    <div class="card" style="width: 300px;">
    <div class="card-divider">
        <h4><%= t(".contact") %></h4>
    </div>
    <div class="card-section">
        <%= render 'form', contact: @contact %>
    </div>
    </div>
</div>