<%= form_with(model: contact) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
        <% contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field t(:name), name: "contact[name]", placeholder:t("user_label_placeholder" ) %>
  </div>

  <div class="field">
    <%= form.text_field t(:email), name: "contact[email]", placeholder:t("email_label_placeholder") %>
  </div>

  <div class="field">
    <%= form.text_field t(:subject), name: "contact[subject]", placeholder:t("subject_label_placeholder") %>
  </div>

  <div class="field">
    <%= form.text_area t(:message), name: "contact[message]", placeholder:t("message_label_placeholder") %>
  </div>

  <div class="actions">
    <%= form.submit t("submit")%>
  </div>
<% end %>
